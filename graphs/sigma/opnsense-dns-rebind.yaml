spec_version: cacao-2.0
name: opnsense-dns-rebind
description: |
  Sigma "OPNsense possible DNS-rebind attack" (opnsense-dns-006) : attaque DNS-rebind detectee par unbound — pivot reseau potentiel.
  INCIDENT high — restaurer le controle + investiguer l acteur (revert manuel cote infra).
trigger:
  sigma_rule: opnsense-dns-006
steps:
  start: { type: start, on_completion: emit_incident }
  emit_incident:
    type: action
    command: { type: threatclaw-emit-incident, severity: high }
    on_completion: terminal
  terminal: { type: end }
