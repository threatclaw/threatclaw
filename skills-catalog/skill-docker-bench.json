{
  "id": "skill-docker-bench",
  "name": "Docker Bench Security",
  "version": "1.0.0",
  "description": "Audit de securite Docker conforme CIS Benchmark. Verifie la configuration du daemon, les images, les containers, les volumes, les reseaux.",
  "description_en": "Docker security audit aligned with the CIS Benchmark. Checks daemon configuration, images, containers, volumes and networks.",
  "help": "Docker Bench Security audite votre installation Docker selon le benchmark CIS (Center for Internet Security). Verifie la configuration du daemon Docker, les permissions des fichiers de configuration, la securite des conteneurs en cours d'execution (privileges, capabilities, volumes sensibles montes), la configuration reseau, et les bonnes pratiques pour les images. Produit un rapport PASS/WARN/FAIL pour chaque controle. Indispensable si vous hebergez des applications en conteneurs.\n\n⚡ DECLENCHEMENT MANUEL\n\nScan on-demand, pas de planification automatique (aucune config requise) :\n• Dashboard > Skills > Docker Bench > bouton « Lancer »\n• API : POST /api/tc/skills/run/skill-docker-bench\n\n⚠️ PORTEE DU SCAN\n\nDocker Bench audite UNIQUEMENT le daemon Docker du serveur ThreatClaw lui-meme (via montage du socket /var/run/docker.sock). Si vos conteneurs de production tournent sur d'autres hotes, installez docker-bench sur chacun d'eux manuellement ou deployez ThreatClaw sur chaque hote Docker a auditer. Cette skill n'est PAS un scanner multi-hote.",
  "help_en": "Docker Bench Security audits your Docker installation against the CIS (Center for Internet Security) benchmark. It checks the Docker daemon configuration, configuration-file permissions, the security of running containers (privileges, capabilities, sensitive mounted volumes), the network configuration, and image best practices. It produces a PASS/WARN/FAIL report for each control. Indispensable if you host containerized applications.\n\n⚡ MANUAL TRIGGER\n\nOn-demand scan, no automatic scheduling (no config required):\n• Dashboard > Skills > Docker Bench > \"Run\" button\n• API: POST /api/tc/skills/run/skill-docker-bench\n\n⚠️ SCAN SCOPE\n\nDocker Bench audits ONLY the Docker daemon of the ThreatClaw server itself (by mounting the /var/run/docker.sock socket). If your production containers run on other hosts, install docker-bench on each of them manually or deploy ThreatClaw on each Docker host to audit. This skill is NOT a multi-host scanner.",
  "author": "ThreatClaw",
  "trust": "official",
  "type": "tool",
  "category": "scan",
  "execution": {
    "mode": "ephemeral",
    "docker_image": "docker/docker-bench-security:latest",
    "command": "docker-bench-security",
    "timeout_seconds": 300,
    "network": "none",
    "memory_limit": "256m",
    "volumes": [
      "/var/run/docker.sock:/var/run/docker.sock:ro",
      "/etc:/host/etc:ro",
      "/var/lib:/host/var/lib:ro"
    ]
  },
  "config": {},
  "outputs": {
    "findings": "db"
  },
  "default_active": false,
  "requires_config": false,
  "api_key_required": false,
  "icon": "container"
}