{
  "id": "skill-fortinet",
  "name": "Fortinet FortiGate",
  "version": "1.0.0",
  "description": "Synchronise la table ARP, les interfaces et VLANs depuis FortiGate. Peut bloquer des IPs via l'API.",
  "description_en": "Syncs the ARP table, interfaces and VLANs from FortiGate. Can block IPs via the API.",
  "author": "ThreatClaw",
  "trust": "official",
  "type": "connector",
  "category": "network",
  "help": "FortiGate est le firewall de Fortinet, très répandu en PME. Il expose une API REST complète permettant de récupérer la table ARP, les interfaces réseau, les VLANs, les routes et les sessions actives.\n\nThreatClaw interroge l'API FortiGate pour cartographier votre réseau dans le graphe. En mode remédiation (avec validation humaine), il peut aussi créer des règles de blocage d'IP via des address objects.\n\nPrérequis :\n• FortiGate accessible via HTTPS (port 443 par défaut)\n• Un API Token : System > Administrators > Create New > REST API Admin\n• Profil admin avec droits de lecture (au minimum Read sur Network, System, Firewall)",
  "help_en": "FortiGate is Fortinet's firewall, very common in SMBs. It exposes a full REST API to retrieve the ARP table, network interfaces, VLANs, routes and active sessions.\n\nThreatClaw queries the FortiGate API to map your network into the graph. In remediation mode (with human approval), it can also create IP-blocking rules via address objects.\n\nPrerequisites:\n• FortiGate reachable over HTTPS (port 443 by default)\n• An API Token: System > Administrators > Create New > REST API Admin\n• An admin profile with read rights (at least Read on Network, System, Firewall)",
  "execution": {
    "mode": "persistent",
    "sync_interval_minutes": 5,
    "protocol": "https"
  },
  "config": {
    "url": {
      "type": "string",
      "description": "URL du FortiGate",
      "description_en": "FortiGate URL",
      "placeholder": "https://192.168.1.1",
      "hint": "L'adresse de votre FortiGate. Utilisez https:// et l'IP de l'interface de management.",
      "hint_en": "The address of your FortiGate. Use https:// and the management interface IP.",
      "required": true
    },
    "api_key": {
      "type": "password",
      "description": "API Key FortiGate",
      "description_en": "FortiGate API Key",
      "hint": "Créez un admin REST API dans FortiGate : System > Administrators > Create New > REST API Admin. Copiez le token généré. Attribuez un profil avec des droits de lecture uniquement.",
      "hint_en": "Create a REST API admin in FortiGate: System > Administrators > Create New > REST API Admin. Copy the generated token. Assign a profile with read-only rights.",
      "required": true
    },
    "no_tls_verify": {
      "type": "boolean",
      "description": "Ignorer le certificat TLS",
      "description_en": "Skip the TLS certificate",
      "hint": "FortiGate utilise un certificat auto-signé par défaut. Laissez activé sauf si vous avez installé un certificat de confiance.",
      "hint_en": "FortiGate uses a self-signed certificate by default. Leave enabled unless you have installed a trusted certificate.",
      "default": true
    }
  },
  "hitl_actions": {
    "enabled": true,
    "implemented": true,
    "requires_separate_creds": false,
    "credential_fields": {},
    "actions": [
      {
        "name": "fortinet_block_ip",
        "label": "Bloquer une IP",
        "label_en": "Block an IP",
        "description": "Crée un address object FortiGate pour l'IP cible et l'attache à une policy DROP en sortie de la zone WAN. Réversible (retrait du membre + suppression de l'address).",
        "description_en": "Creates a FortiGate address object for the target IP and attaches it to a DROP policy outbound from the WAN zone. Reversible (member removal + address deletion)."
      },
      {
        "name": "fortinet_block_url",
        "label": "Bloquer une URL",
        "label_en": "Block a URL",
        "description": "Ajoute le pattern à un profil webfilter (catégorie blocked-urls) appliqué aux policies sortantes. Réversible. Nécessite un profil UTM webfilter actif.",
        "description_en": "Adds the pattern to a webfilter profile (blocked-urls category) applied to the outbound policies. Reversible. Requires an active UTM webfilter profile."
      }
    ]
  },
  "auto_sync": true,
  "sync_interval": 5,
  "outputs": {
    "assets": "graph"
  },
  "default_active": false,
  "requires_config": true,
  "api_key_required": true,
  "icon": "shield"
}
