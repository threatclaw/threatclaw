{
  "id": "skill-pfsense",
  "name": "pfSense",
  "version": "1.0.0",
  "description": "Connecte ThreatClaw au firewall pfSense en lecture seule (via le paquet pfSense-pkg-API v2). Importe ARP, baux DHCP, interfaces, VLANs et règles. Alimente automatiquement /assets : chaque machine vue par pfSense apparaît avec son MAC + hostname, puis se fait fingerprinter par Nmap dans la foulée.",
  "description_en": "Connects ThreatClaw to the pfSense firewall read-only (via the pfSense-pkg-API v2 package). Imports ARP, DHCP leases, interfaces, VLANs and rules. Automatically feeds /assets: every host seen by pfSense appears with its MAC + hostname, then gets fingerprinted by Nmap right after.",
  "author": "ThreatClaw",
  "trust": "official",
  "type": "connector",
  "category": "network",
  "help": "À QUOI ÇA SERT\n\npfSense voit tout le trafic du LAN, donc il connaît mieux que personne ton parc interne. ThreatClaw exploite ça pour cartographier sans poser d'agent. À chaque sync (5 min) on récupère :\n\n• ARP table — chaque IP/MAC actif (utile pour repérer les machines orphelines)\n• DHCP leases — baux actifs avec hostname auto-déclaré\n• Interfaces — liste, MTU, état link\n• VLANs — segmentation\n• Règles firewall — comptées\n\nLes nouvelles IP non connues deviennent des assets dans /assets. Un scan Nmap automatique se déclenche derrière (TTL 1h) pour les fingerprinter.\n\nMODE D'EMPLOI\n\n1. Installer le paquet API : pfSense UI → System → Package Manager → Available Packages → chercher 'pfSense-pkg-RESTAPI' → Install. C'est le paquet officiel maintenu par jaredhendrickson13/pfrest, requis car pfSense de base n'expose pas d'API REST.\n2. Créer un user API dédié : System → User Manager → +\n   - Username : threatclaw\n   - Password : (générer un mot de passe fort)\n   - Group memberships : aucun (read-only)\n3. Donner le privilège lecture API au user : Edit user → Effective Privileges → + → 'WebCfg - System: API package' (ou 'WebCfg - All pages' en lecture si tu veux scoper plus large)\n4. Coller le username dans 'Utilisateur API' ci-dessous et le password dans 'Secret API'\n5. Laisser 'Ignorer la vérification TLS' coché si pfSense utilise un certificat auto-signé\n\nVERSIONS TESTÉES\n\npfSense CE 2.7+ et pfSense Plus 23.05+ avec pfSense-pkg-RESTAPI v2." ,
  "help_en": "WHAT IT'S FOR\n\npfSense sees all the LAN traffic, so it knows your internal estate better than anyone. ThreatClaw uses this to map without deploying an agent. On each sync (5 min) we pull:\n\n• ARP table — every active IP/MAC (useful for spotting orphan machines)\n• DHCP leases — active leases with self-declared hostname\n• Interfaces — list, MTU, link state\n• VLANs — segmentation\n• Firewall rules — counted\n\nNew unknown IPs become assets in /assets. An automatic Nmap scan fires behind (TTL 1h) to fingerprint them.\n\nHOW TO USE\n\n1. Install the API package: pfSense UI → System → Package Manager → Available Packages → search 'pfSense-pkg-RESTAPI' → Install. This is the official package maintained by jaredhendrickson13/pfrest, required because base pfSense exposes no REST API.\n2. Create a dedicated API user: System → User Manager → +\n   - Username: threatclaw\n   - Password: (generate a strong password)\n   - Group memberships: none (read-only)\n3. Grant the API read privilege to the user: Edit user → Effective Privileges → + → 'WebCfg - System: API package' (or 'WebCfg - All pages' read-only if you want a broader scope)\n4. Paste the username into 'API user' below and the password into 'API secret'\n5. Leave 'Skip TLS verification' checked if pfSense uses a self-signed certificate\n\nTESTED VERSIONS\n\npfSense CE 2.7+ and pfSense Plus 23.05+ with pfSense-pkg-RESTAPI v2.",
  "execution": {
    "mode": "persistent",
    "sync_interval_minutes": 5,
    "protocol": "https"
  },
  "config": {
    "url": {
      "type": "string",
      "description": "URL pfSense",
      "placeholder": "https://10.0.0.1",
      "hint": "Adresse de la GUI pfSense (sans /api). Utilise https://. L'IP de l'interface LAN typiquement.",
      "required": true
    },
    "auth_user": {
      "type": "string",
      "description": "Utilisateur API",
      "placeholder": "threatclaw",
      "hint": "Le nom du user API que tu as créé dans System > User Manager.",
      "required": true
    },
    "auth_secret": {
      "type": "password",
      "description": "Mot de passe",
      "hint": "Mot de passe du user API. Stocké chiffré côté ThreatClaw.",
      "required": true
    },
    "no_tls_verify": {
      "type": "boolean",
      "description": "Ignorer la vérification du certificat TLS",
      "hint": "pfSense utilise un certificat auto-signé par défaut. Laisse activé sauf si tu as un Let's Encrypt ou une PKI interne sur la GUI.",
      "default": true
    }
  },
  "hitl_actions": {
    "enabled": true,
    "implemented": true,
    "requires_separate_creds": false,
    "credential_fields": {},
    "actions": [
      {
        "name": "pfsense_block_ip",
        "label": "Bloquer une IP",
        "description": "Ajoute une IP à un alias firewall et applique la règle de drop. Réversible (suppression de l'alias = lift)."
      }
    ]
  },
  "auto_sync": true,
  "sync_interval": 5,
  "outputs": {
    "assets": "graph",
    "topology": "graph"
  },
  "default_active": false,
  "requires_config": true,
  "api_key_required": false,
  "icon": "shield"
}
