{
  "id": "skill-trufflehog",
  "name": "TruffleHog Secrets Scanner",
  "version": "1.0.0",
  "description": "Detection de secrets, cles API, tokens dans le code source et les repositories Git.",
  "description_en": "Detection of secrets, API keys and tokens in source code and Git repositories.",
  "help": "TruffleHog parcourt vos depots Git et repertoires pour detecter les secrets exposes : cles API AWS/GCP/Azure, tokens GitHub/GitLab, mots de passe en dur, cles privees SSH/TLS, credentials de bases de donnees. Analyse tout l'historique Git pour retrouver les secrets meme supprimes. Chaque secret detecte est verifie automatiquement (quand possible) pour confirmer s'il est encore actif. Indispensable pour eviter les fuites de credentials.\n\n⚡ DECLENCHEMENT MANUEL\n\nScan on-demand, pas de planification automatique :\n• Dashboard > Skills > TruffleHog > bouton « Lancer »\n• API : POST /api/tc/skills/run/skill-trufflehog (pour cron externe)\n\nThreatClaw monte le chemin TARGET dans un conteneur Docker ephemere. Le depot Git (avec son .git/ pour l'historique) doit etre cloné sur le serveur ThreatClaw au prealable. Pour scanner GitHub/GitLab directement, clonez le repo sur le serveur TC avant.",
  "help_en": "TruffleHog scans your Git repositories and directories to detect exposed secrets: AWS/GCP/Azure API keys, GitHub/GitLab tokens, hardcoded passwords, SSH/TLS private keys, database credentials. It analyzes the entire Git history to find secrets even after deletion. Each detected secret is automatically verified (when possible) to confirm whether it is still active. Indispensable to prevent credential leaks.\n\n⚡ MANUAL TRIGGER\n\nOn-demand scan, no automatic scheduling:\n• Dashboard > Skills > TruffleHog > \"Run\" button\n• API: POST /api/tc/skills/run/skill-trufflehog (for an external cron)\n\nThreatClaw mounts the TARGET path into an ephemeral Docker container. The Git repository (with its .git/ for history) must be cloned on the ThreatClaw server beforehand. To scan GitHub/GitLab directly, clone the repo on the TC server first.",
  "author": "ThreatClaw",
  "trust": "official",
  "type": "tool",
  "category": "scan",
  "execution": {
    "mode": "ephemeral",
    "docker_image": "trufflesecurity/trufflehog:latest",
    "command": "trufflehog filesystem {TARGET} --json",
    "timeout_seconds": 300,
    "network": "none",
    "memory_limit": "512m"
  },
  "config": {
    "target": {
      "type": "string",
      "description": "Chemin du depot Git ou repertoire a scanner",
      "description_en": "Path to the Git repository or directory to scan",
      "hint": "Chemin absolu vers le depot Git ou le repertoire contenant le code source a analyser. L'historique Git complet sera parcouru si c'est un depot Git.",
      "hint_en": "Absolute path to the Git repository or directory containing the source code to analyze. The full Git history will be scanned if it's a Git repository.",
      "placeholder": "/opt/monapp",
      "required": true
    }
  },
  "outputs": {
    "findings": "db"
  },
  "default_active": false,
  "requires_config": true,
  "api_key_required": false,
  "icon": "key",
  "advanced": true
}