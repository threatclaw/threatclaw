{
  "id": "skill-glpi",
  "name": "GLPI CMDB",
  "version": "1.1.0",
  "description": "Importe les assets depuis GLPI (ordinateurs, équipements réseau) et permet de créer des tickets d'incident depuis les findings ThreatClaw.",
  "description_en": "Imports assets from GLPI (computers, network equipment) and can create incident tickets from ThreatClaw findings.",
  "author": "ThreatClaw",
  "trust": "official",
  "type": "connector",
  "category": "inventory",
  "help": "GLPI est un outil open-source de gestion de parc informatique (CMDB) et de helpdesk/ticketing.\n\nThreatClaw s'y connecte pour :\n• Importer l'inventaire des machines (ordinateurs, serveurs, équipements réseau) comme assets\n• Créer des tickets d'incident automatiquement quand un finding critique est détecté\n\nPrérequis :\n• GLPI avec l'API REST activée (Configuration > Générale > API)\n• Un App-Token (Configuration > Générale > API > Clé d'application)\n• Un User-Token (Administration > Utilisateurs > glpi > Clés d'accès distantes)\n• L'adresse IP de ThreatClaw autorisée dans les clients API GLPI",
  "help_en": "GLPI is an open-source IT asset management (CMDB) and helpdesk/ticketing tool.\n\nThreatClaw connects to it to:\n• Import the machine inventory (computers, servers, network equipment) as assets\n• Automatically create incident tickets when a critical finding is detected\n\nPrerequisites:\n• GLPI with the REST API enabled (Setup > General > API)\n• An App-Token (Setup > General > API > Application key)\n• A User-Token (Administration > Users > glpi > Remote access keys)\n• ThreatClaw's IP address allowed in the GLPI API clients",
  "execution": {
    "mode": "persistent",
    "sync_interval_minutes": 60,
    "protocol": "https"
  },
  "config": {
    "glpi_url": {
      "type": "string",
      "description": "URL de GLPI",
      "description_en": "GLPI URL",
      "placeholder": "http://192.168.1.10:9090",
      "hint": "L'adresse de votre serveur GLPI. L'API REST doit être activée dans Configuration > Générale > API.",
      "hint_en": "The address of your GLPI server. The REST API must be enabled in Setup > General > API.",
      "required": true
    },
    "glpi_app_token": {
      "type": "password",
      "description": "App-Token GLPI",
      "description_en": "GLPI App-Token",
      "hint": "Le token d'application GLPI. Se trouve dans Configuration > Générale > API. Nécessaire pour toute requête API.",
      "hint_en": "The GLPI application token. Found in Setup > General > API. Required for any API request.",
      "required": true
    },
    "glpi_user_token": {
      "type": "password",
      "description": "User-Token GLPI",
      "description_en": "GLPI User-Token",
      "hint": "Le token utilisateur. Se trouve dans Administration > Utilisateurs > votre user > Clés d'accès distantes > Régénérer.",
      "hint_en": "The user token. Found in Administration > Users > your user > Remote access keys > Regenerate.",
      "required": true
    },
    "no_tls_verify": {
      "type": "boolean",
      "description": "Ignorer le certificat TLS",
      "description_en": "Skip the TLS certificate",
      "hint": "Activez si GLPI utilise un certificat auto-signé.",
      "hint_en": "Enable if GLPI uses a self-signed certificate.",
      "default": false
    },
    "auto_ticket": {
      "type": "boolean",
      "description": "Créer automatiquement des tickets pour les findings CRITICAL",
      "description_en": "Automatically create tickets for CRITICAL findings",
      "hint": "Si activé, ThreatClaw crée un ticket GLPI pour chaque finding de sévérité CRITICAL confirmé par le pipeline d'investigation.",
      "hint_en": "If enabled, ThreatClaw creates a GLPI ticket for each CRITICAL-severity finding confirmed by the investigation pipeline.",
      "default": false
    }
  },
  "outputs": {
    "assets": "graph",
    "tickets": "glpi"
  },
  "default_active": false,
  "requires_config": true,
  "api_key_required": false,
  "icon": "database"
}
